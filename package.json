{
  "scripts": {
    "build": "yarn && tsc && rm -rf node_modules/ && yarn --production"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "libnpmpublish": "^3.0.1"
  },
  "devDependencies": {
    "@npm/types": "^1.0.1",
    "@types/node": "^14.0.5"
  }
}
